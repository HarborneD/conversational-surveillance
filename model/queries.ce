[RegionDisplaySearch]
for which V1 and V2 is it true that 
    ( the displayable thing V1 can show the region V2 )
.

[LocationDisplaySearch]
for which DISP and LOC is it true that 
    ( the displayable thing DISP can show the location LOC )
.

