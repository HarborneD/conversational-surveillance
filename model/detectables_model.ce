conceptualise a ~ detectable object ~ DET that
    is a geolocation source and
    ~ is located at ~ the location loc and
    has the value DISP as ~ object display name ~ and
    has the value PROPS as ~ display properties ~
.